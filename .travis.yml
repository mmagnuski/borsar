language: c
dist: xenial
sudo: required

env:
  global: PYTHON_VERSION=3.7 TRAVIS_PYTHON_VERSION=3.7 CONDA_VERSION=">=4.3.27"
          CONDA_ENVIRONMENT="environment.yml" DEBUG=True

matrix:
  include:
    - os: linux
      env: USE_NUMBA="yes"

    - os: linux
      env: USE_NUMBA="no"

    - os: osx
      env: USE_NUMBA="no"

before_install:
  - if [ "${TRAVIS_OS_NAME}" == "osx" ]; then
      brew update;
    else
      sudo apt-get update;
    fi
  - curl -O https://raw.githubusercontent.com/mne-tools/mne-python/master/environment.yml
  - git clone https://github.com/astropy/ci-helpers.git
  - source ci-helpers/travis/setup_conda.sh

install:
  # Rvm overrides cd with a function so that it can hook into it to run
  # some scripts, see https://github.com/travis-ci/travis-ci/issues/8703
  - if [ "${TRAVIS_OS_NAME}" == "osx" ]; then
      unset -f cd;
    fi;
  - hash -r
  # Useful for debugging any issues with conda
  - conda info -a

  # install dependencies
  - if [ "${USE_NUMBA}" == "yes" ]; then
      conda install numba;
    fi
  - pip install codecov tqdm scikit-image
  - python setup.py develop

script:
  # Your test script goes here
  - pytest --cov=borsar borsar/

after_success:
  - codecov
